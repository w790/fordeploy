# Fordeploy - Учебный проект: Docker + Nginx для начинающих

## 📚 О проекте

Этот проект создан для изучения Docker и Nginx с нуля. Здесь я разбираюсь, как упаковывать Django приложение в контейнеры и разворачивать его на сервере.

## 🎯 Что я изучаю

- **Docker** - что это такое и как работает
- **Nginx** - как настроить веб-сервер
- **Docker Compose** - как управлять несколькими контейнерами
- **Деплой** - как закинуть проект на сервер и запустить

## 📁 Структура моего проекта

```
fordeploy/
├── .venv/                    # Виртуальное окружение Python
├── cart/                     # Приложение корзины
├── letsencrypt/              # Папка для SSL сертификатов
├── main/                     # Главное приложение
├── media/                    # Медиа файлы
├── orders/                   # Приложение заказов
├── product/                  # Приложение товаров
├── reverence/                # Настройки Django проекта
├── static/                   # Статические файлы
├── users/                    # Приложение пользователей
├── .env                      # Локальные настройки
├── .env.docker               # Настройки для Docker
├── docker-compose.yml        # Настройка всех контейнеров
├── Dockerfile                # Инструкция сборки приложения
├── nginx.conf                # Настройки Nginx
├── manage.py                 # Django управление
└── requirements.txt          # Библиотеки Python
```

## 🐳 Что такое Docker?

**Docker** - это система, которая упаковывает приложение со всеми зависимостями в "контейнер". 

Представь: 
- **Без Docker**: "На сервере нет нужных библиотек, версии не те, ничего не работает"
- **С Docker**: "Вот контейнер - работает везде где есть Docker"

## 🌐 Что такое Nginx?

**Nginx** - это веб-сервер, который:
- Принимает запросы от пользователей
- Раздает статические файлы (картинки, CSS, JS)
- Передает динамические запросы в Django
- Может работать с HTTPS

## 🔧 Мои конфигурационные файлы

### 1. Docker Compose (docker-compose.yml)
```yaml
services:
  web:          # Мое Django приложение
    build: .    # Собираем из Dockerfile
    ports: ["8000:8000"]
    env_file: [".env.docker"]
    volumes:
      - .:/app              # Синхронизация кода
      - static:/app/static  # Общие статические файлы
      - media:/app/media    # Общие медиа файлы
    depends_on:
      - db                  # Ждем базу данных

  db:           # База данных PostgreSQL
    image: postgres:13
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data/

  nginx:        # Веб-сервер
    image: nginx:latest
    ports: ["80:80", "443:443"]
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./letsencrypt:/etc/letsencrypt
      - static:/app/static
      - media:/app/media
    depends_on:
      - web
```

### 2. Dockerfile
```dockerfile
FROM python:3.10-slim  # Базовый образ Python

WORKDIR /app  # Рабочая папка в контейнере

# Устанавливаем системные зависимости
RUN apt-get update && apt-get install -y \
    libpq-dev \    # Для работы с PostgreSQL
    gcc \          # Для компиляции Python пакетов
    gettext \      # Для перевода текстов
    vim \          # Редактор для отладки
    && rm -rf /var/lib/apt/lists/*

# Копируем и устанавливаем Python библиотеки
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копируем весь код приложения
COPY . .

# Запускаем приложение
CMD ["sh", "-c", "python manage.py migrate && gunicorn reverence.wsgi:application --bind 0.0.0.0:8000"]
```

### 3. Nginx конфигурация (nginx.conf)
```nginx
events {}

http {
    server {
        listen 80;          # Слушаем порт 80 (HTTP)
        server_name localhost;  # Имя сервера

        # Все запросы передаем в Django
        location / {
            proxy_pass http://web:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Статические файлы раздаем напрямую
        location /static/ {
            alias /app/static/;
        }

        # Медиа файлы раздаем напрямую
        location /media/ {
            alias /app/media/;
        }
    }
}
```

## 🚀 Как запустить проект локально

### 1. Убедись что установлен Docker
Скачай с [docker.com](https://www.docker.com/products/docker-desktop)

### 2. Настрой окружение
```bash
# Создай файл .env.docker
echo "SECRET_KEY=твой-секретный-ключ
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
POSTGRES_DB=fordeploy_db
POSTGRES_USER=fordeploy_user
POSTGRES_PASSWORD=fordeploy_password
POSTGRES_HOST=db" > .env.docker
```

### 3. Запусти проект
```bash
# Собери и запусти все контейнеры
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

### 4. Проверь что работает
- Открой http://localhost - приложение через Nginx
- Открой http://localhost:8000 - приложение напрямую

## 🌐 Как закинуть на сервер

### Шаг 1: Купи VPS сервер
- Зарегистрируйся на DigitalOcean, TimeWeb, Selectel
- Выбери самый дешевый VPS (от 200-300 руб/мес)
- Установи Ubuntu 20.04/22.04

### Шаг 2: Купи домен
- Любой домен в зоне .ru, .com и т.д. (от 200 руб/год)
- В настройках домена укажи A-запись на IP твоего сервера

### Шаг 3: Подключись к серверу
```bash
ssh root@ip-твоего-сервера
```

### Шаг 4: Установи Docker на сервер
```bash
# На сервере выполни:
apt update && apt upgrade -y
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
apt install docker-compose-plugin -y
```

### Шаг 5: Скопируй проект на сервер
```bash
# С локального компьютера:
scp -r ./fordeploy root@ip-твоего-сервера:/opt/
```

### Шаг 6: Настрой для продакшена
На сервере в папке `/opt/fordeploy`:

**Измени nginx.conf**:
```nginx
server {
    listen 80;
    server_name ТВОЙ-ДОМЕН.ru www.ТВОЙ-ДОМЕН.ru;  # ТВОЙ ДОМЕН!
    # ... остальное без изменений
}
```

**Измени .env.docker**:
```bash
DEBUG=False
ALLOWED_HOSTS=ТВОЙ-ДОМЕН.ru,www.ТВОЙ-ДОМЕН.ru,ip-твоего-сервера
```

### Шаг 7: Запусти на сервере
```bash
cd /opt/fordeploy
docker-compose up -d --build
```

### Шаг 8: Настрой HTTPS (опционально)
```bash
# На сервере:
apt install certbot python3-certbot-nginx -y
certbot --nginx -d ТВОЙ-ДОМЕН.ru -d www.ТВОЙ-ДОМЕН.ru
```

## 🛠 Полезные команды

### Для разработки
```bash
# Запустить все
docker-compose up --build

# Остановить все
docker-compose down

# Посмотреть логи
docker-compose logs
docker-compose logs web
docker-compose logs nginx

# Зайти в контейнер
docker-compose exec web bash
```

### Для администрирования
```bash
# Создать админа
docker-compose exec web python manage.py createsuperuser

# Применить миграции
docker-compose exec web python manage.py migrate

# Собрать статику
docker-compose exec web python manage.py collectstatic --noinput
```

### Для мониторинга
```bash
# Статус контейнеров
docker-compose ps

# Использование ресурсов
docker stats

# Последние логи
docker-compose logs --tail=50
```

## ❗ Частые проблемы

### "Статика не грузится"
```bash
docker-compose exec web python manage.py collectstatic --noinput
```

### "База данных не подключилась"
```bash
docker-compose restart db
# Подожди 10 секунд
docker-compose restart web
```

### "Порт 80 занят"
Убедись что на сервере не запущен другой веб-сервер.

### "Docker не запускается"
Проверь что Docker Desktop запущен на твоем компьютере.

## 🎓 Чему я научился

- ✅ **Docker**: Упаковывать приложения в контейнеры
- ✅ **Docker Compose**: Управлять несколькими сервисами
- ✅ **Nginx**: Настраивать веб-сервер и прокси
- ✅ **Деплой**: Разворачивать приложения на сервере
- ✅ **Базы данных**: Работать с PostgreSQL в Docker
- ✅ **Домены**: Настраивать доменные имена

## 📈 Что дальше?

После этого проекта можно изучить:
- GitHub Actions для автоматического деплоя
- Redis для кэширования
- Мониторинг приложения
- Балансировку нагрузки

---
